<Window x:Class="AtelierApp.Pages.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена пароля" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Смена пароля" 
                   FontSize="18" FontWeight="Bold" 
                   Grid.Row="0" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Текущий пароль:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="CurrentPasswordBox" Margin="0,0,0,15" Height="30"/>

            <TextBlock Text="Новый пароль:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="NewPasswordBox" Margin="0,0,0,15" Height="30"/>

            <TextBlock Text="Подтверждение пароля:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,0,0,15" Height="30"/>

            <TextBlock x:Name="ErrorTextBlock" Foreground="Red" 
                       TextWrapping="Wrap" Visibility="Collapsed"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Сохранить" 
                    Click="SaveButton_Click" 
                    Height="35" Background="LightGreen"/>
            <Button Grid.Column="2" Content="Отмена" 
                    Click="CancelButton_Click" 
                    Height="35" Background="LightPink"/>
        </Grid>
    </Grid>
</Window>