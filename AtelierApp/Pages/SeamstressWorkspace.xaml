<Page x:Class="AtelierApp.Pages.SeamstressWorkspace"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AtelierApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SeamstressWorkspace">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Мои задания" FontSize="24" FontWeight="Bold" 
                   Grid.Row="0" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Фильтр:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}"
                      Width="150" Margin="0,0,10,0"/>
            <Button Content="Показать" Command="{Binding FilterCommand}" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding MyTasks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="№ заказа" DisplayMemberBinding="{Binding OrderID}" Width="80"/>
                    <GridViewColumn Header="Описание" DisplayMemberBinding="{Binding Description}" Width="300"/>
                    <GridViewColumn Header="Срок" DisplayMemberBinding="{Binding Deadline, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding StatusName}" Width="120"/>
                    <GridViewColumn Header="Действие">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Выполнено" Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding ID}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
